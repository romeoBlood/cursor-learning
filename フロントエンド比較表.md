# フロントエンド技術選定 比較表

## 前提条件
- SEO不要
- サーバ処理なし（バックエンドAPIからJSONを受け取って表示）
- 大規模アプリケーション

## 比較表

| 観点 | Vanilla JS | React (Vite) | Next.js |
|------|-----------|---------------|---------|
| **UI構築** | 手続き的（DOM直接操作） | 宣言的（状態→UI自動反映） | 宣言的（React準拠） |
| **コンポーネント再利用** | 自前でテンプレート管理が必要 | コンポーネント単位で自然に分割・再利用 | React準拠 |
| **状態管理** | イベントリスナー+DOM書き換えを手動で紐付け | useState/useReducer で変更時に自動再レンダリング | React準拠 + Server State管理も可 |
| **ルーティング** | 自前実装 or ライブラリ選定が必要 | react-router で十分 | ファイルベースルーティング（自動） |
| **データフェッチ (JSON)** | fetch API（受け取り自体は同じ） | fetch + TanStack Query 等で状態管理と統合 | Server Component / Client Component の使い分けが必要 |
| **TypeScript対応** | DOM操作に型をつけるのが困難 | Props/Stateに型がつき安全 | React準拠 |
| **エコシステム** | 限定的 | MUI, shadcn/ui, React Hook Form, Framer Motion 等豊富 | Reactエコシステム + Next.js固有機能 |
| **ビルド設定** | 不要（そのまま動く） | Viteでシンプル | Next.js固有の設定・規約あり |
| **学習コスト** | 低（JS基礎のみ） | 中（React概念の習得が必要） | 高（App Router, RSC, Server/Client境界の理解が必要） |
| **DevTools** | console.log + Elements手動確認 | React DevToolsでstate/props可視化 | React DevTools + Next.js固有デバッグ |
| **バンドルサイズ** | ゼロ | Reactランタイム分あり（軽量） | React + Next.jsランタイム |
| **保守性（大規模時）** | ✕ 破綻しやすい（自作FW化する） | ◎ コンポーネント分割で見通し良い | ◎ ただしSSR不要なら過剰な場合あり |
| **チーム開発** | ✕ 規約が属人化しやすい | ◎ コンポーネント単位で分業しやすい | ◎ 規約が明確 |
| **採用・情報量** | 少（UI設計の情報が薄い） | 圧倒的に多い | 多い |
| **SSR / SSG** | 非対応 | 非対応（必要ならNext.jsへ移行可） | 対応（ただし今回は不要） |
| **SEO** | 弱い（CSRのみ） | 弱い（CSRのみ） | 強い（SSR/SSG対応。ただし今回は不要） |

## 結論

**SEO不要 × サーバ処理なし × 大規模アプリ** の条件では **React (Vite + react-router)** が最適解。

- Vanilla JS → 大規模では状態管理・DOM操作の保守が破綻する
- Next.js → SSR/SSG/SEO が不要なら、App Routerの複雑さがオーバーヘッドになる
- **React (Vite)** → 必要十分な機能をシンプルに使える。将来SSRが必要になればNext.jsへの移行も可能
